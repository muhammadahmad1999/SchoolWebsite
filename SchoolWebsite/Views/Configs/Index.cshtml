@model IEnumerable<SchoolWebsite.Models.Config>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SystemID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RolesAllowed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SystemID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Action)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RolesAllowed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConfigID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConfigID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConfigID })
        </td>
    </tr>
}

</table>
